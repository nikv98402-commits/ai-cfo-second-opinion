generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model UserProfile {
  id             String   @id @default(cuid())
  supabaseUserId String   @unique
  email          String   @unique
  fullName       String?
  role           String   @default("founder")
  locale         String   @default("ru")
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  companies Company[]
  cases     Case[]
}

model Company {
  id                 String   @id @default(cuid())
  ownerUserId        String
  owner              UserProfile @relation(fields: [ownerUserId], references: [id], onDelete: Cascade)
  name               String
  industry           String
  annualRevenue      Float?
  employeesCount     Int?
  legalEntitiesCount Int?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  cases Case[]

  @@index([ownerUserId])
}

model Case {
  id                 String   @id @default(cuid())
  ownerUserId        String?
  owner              UserProfile? @relation(fields: [ownerUserId], references: [id], onDelete: Cascade)
  companyId          String?
  company            Company? @relation(fields: [companyId], references: [id], onDelete: Cascade)
  companyName        String
  industry           String
  annualRevenue      Float
  employeesCount     Int
  legalEntitiesCount Int
  hasDebt            Boolean
  hasCapexPlan       Boolean
  financeOwner       String
  reportsAvailableJson String
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  financialInputs FinancialInput[]
  analysisResults AnalysisResult[]
  uploadedFiles   UploadedFile[]
  evidenceItems   EvidenceItem[]
  ownerBriefs     OwnerBrief[]
  chatMessages    ChatMessage[]

  @@index([ownerUserId])
  @@index([companyId])
}

model UploadedFile {
  id              String   @id @default(cuid())
  caseId          String
  case            Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  slot            String
  originalName    String
  mimeType        String?
  storageKey      String
  sheetNamesJson      String
  detectedColumnsJson String
  previewRowsJson     String
  status          String   @default("uploaded")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  mappingProfiles MappingProfile[]

  @@index([caseId])
}

model MappingProfile {
  id             String       @id @default(cuid())
  uploadedFileId String
  uploadedFile   UploadedFile @relation(fields: [uploadedFileId], references: [id], onDelete: Cascade)
  slot           String
  sourceSheet    String
  columnMappingJson String
  confidence     Int
  status         String       @default("draft")
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  @@index([uploadedFileId])
}

model FinancialInput {
  id                  String   @id @default(cuid())
  caseId              String
  case                Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  periodName          String
  revenue             Float
  cogs                Float
  grossProfit         Float
  commercialExpenses  Float
  adminExpenses       Float
  payroll             Float
  rent                Float
  ebitda              Float
  depreciation        Float
  ebit                Float
  interestExpense     Float
  netProfit           Float
  operatingCashFlow   Float
  capex               Float
  financingCashFlow   Float
  debtRepayment       Float
  newDebt             Float
  cashStart           Float
  cashEnd             Float
  cash                Float
  accountsReceivable  Float
  inventory           Float
  fixedAssets         Float
  accountsPayable     Float
  shortTermDebt       Float
  longTermDebt        Float
  equity              Float
  avgDebtRate         Float?
  wacc                Float?
  expectedCapexRoi    Float?
  dso                 Float?
  dio                 Float?
  dpo                 Float?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([caseId])
}

model WorkingCapitalAging {
  id          String   @id @default(cuid())
  caseId      String
  periodName  String
  partyName   String
  partyType   String
  amount      Float
  bucket      String
  sourceRef   String
  createdAt   DateTime @default(now())

  @@index([caseId])
}

model DebtSchedule {
  id            String   @id @default(cuid())
  caseId        String
  lender        String
  facilityType  String
  principal     Float
  interestRate  Float?
  dueDate       DateTime?
  nextPayment   Float
  covenantNotes String?
  sourceRef     String
  createdAt     DateTime @default(now())

  @@index([caseId])
}

model CapexPlan {
  id              String   @id @default(cuid())
  caseId          String
  projectName     String
  amount          Float
  expectedRoi     Float?
  fundingSource   String?
  periodName      String
  managementNote  String?
  sourceRef       String
  createdAt       DateTime @default(now())

  @@index([caseId])
}

model SalesChannelMetric {
  id                 String   @id @default(cuid())
  caseId             String
  periodName         String
  channelName        String
  revenue            Float
  grossMargin        Float?
  discountRate       Float?
  logisticsCost      Float?
  marketplaceFee     Float?
  contributionMargin Float?
  sourceRef          String
  createdAt          DateTime @default(now())

  @@index([caseId])
}

model EvidenceItem {
  id          String   @id @default(cuid())
  caseId      String
  case        Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  conclusion  String
  sourceRef   String
  formula     String?
  value       String?
  confidence  Int
  evidenceType String
  createdAt   DateTime @default(now())

  @@index([caseId])
}

model AnalysisResult {
  id                     String   @id @default(cuid())
  caseId                 String
  case                   Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  riskScore              Int
  riskLevel              String
  executiveSummary       String
  metricsJson                String
  redFlagsJson               String
  liquidityRisksJson         String
  operatingLeverageRisksJson String
  capexRisksJson             String
  debtRisksJson              String
  workingCapitalRisksJson    String
  financeMaturityJson        String
  hiringRecommendationJson   String
  questionsToFinanceTeamJson String
  ownerEducationBlock    String
  nextActionsJson            String
  markdownReport         String
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  ownerBriefs OwnerBrief[]

  @@index([caseId])
}

model OwnerBrief {
  id               String   @id @default(cuid())
  caseId           String
  case             Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  analysisResultId String?
  analysisResult   AnalysisResult? @relation(fields: [analysisResultId], references: [id], onDelete: SetNull)
  title            String
  confidence       Int
  verdict          String
  markdownBody     String
  status           String   @default("draft")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([caseId])
  @@index([analysisResultId])
}

model ChatMessage {
  id        String   @id @default(cuid())
  caseId    String
  case      Case     @relation(fields: [caseId], references: [id], onDelete: Cascade)
  role      String
  content   String
  provider  String?
  model     String?
  metadataJson String?
  createdAt DateTime @default(now())

  @@index([caseId])
}
